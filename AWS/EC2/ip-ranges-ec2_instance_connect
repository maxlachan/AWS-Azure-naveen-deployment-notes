curl -s https://ip-ranges.amazonaws.com/ip-ranges.json | jq -r '
  .prefixes[] | select(.service=="EC2_INSTANCE_CONNECT" and .region=="us-east-1") | .ip_prefix
'

For complete IP ranges
https://ip-ranges.amazonaws.com/ip-ranges.json
